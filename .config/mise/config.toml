[tools]
# Docs (mdbook 本体は brew、mermaid プラグインは brew formula が無いため GitHub リリースから取得)
"github:badboy/mdbook-mermaid" = "latest"

# Infrastructure
terraform = "latest"
awscli = { version = "latest", symlink_bins = "true" }

# Kubernetes
kubectl = "latest"
helm = "latest"

# Runtimes
node = "latest"
bun = "latest"
python = "latest"
ruby = "latest"
